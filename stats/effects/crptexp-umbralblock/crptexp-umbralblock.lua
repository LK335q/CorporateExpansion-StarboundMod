function init()
   effect.addStatModifierGroup({{stat = "crptexp-umbralResistance", amount = 0.25}, {stat = "crptexp-umbralStatusImmunity", amount = 1}})

   script.setUpdateDelta(0)
end

function update(dt)

end

function uninit()

end
